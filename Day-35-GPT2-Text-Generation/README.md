# Day 35 — GPT-2 Text Generation (Fine-Tuning With HuggingFace)

This project fine-tunes GPT-2 on a custom dataset and generates new text based on your prompt.

---

## 🚀 Features
- Fine-tune GPT-2 on any text file  
- Save & reuse the trained model  
- Control generation (temperature, top-p, etc.)  
- Explore NLP transfer learning  

---

## 📦 Installation

pip install -r requirements.txt


---

## ▶️ Train the Model


python train_gpt2.py


---

## ▶️ Generate Text

python generate_text.py


---

## 📁 Dataset
Modify `data.txt` to teach GPT-2 your own writing style.

---

## 🎉 Output
We will get a fine-tuned GPT-2 model that generates text similar to your custom dataset.
